az login

# Create a resource group
az group create --n reimbursement-poc-rg -l eastus

# Create app service
az group deployment create -g reimbursement-poc-rg --template-file app-service\deloy-app-service.json --parameters app-service-identity-api\deloy-app-service.parameters.json
az group deployment create -g reimbursement-poc-rg --template-file app-service\deloy-app-service.json --parameters app-service-ui\deloy-app-service.parameters.json

# Create sql database
az group deployment create -g reimbursement-poc-rg --template-file sql\sqldeploy.json --parameters sql\sqldeploy.parameters.json

# Create a container registry
az acr create --resource-group reimbursement-poc-rg --name reimbursementpoccontainerregistry --sku Basic

# Create a Kubernetes cluster
az aks create --resource-group reimbursement-poc-rg --name reimbursement-poc --node-count 1 --enable-addons monitoring --generate-ssh-keys  --node-vm-size Standard_B2s

az group deployment create -g reimbursement-poc-rg --template-file servicebus\sbusdeploy.json --parameters servicebus\sbusdeploy.parameters.json
az group deployment create -g reimbursement-poc-rg --template-file storage\deploystorage.json --parameters storage\deploystorage.parameters.json
az group deployment create -g reimbursement-poc-rg --template-file cosmos\deploycosmos.json --parameters cosmos\deploycosmos.parameters.json
